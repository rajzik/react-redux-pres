import React from 'react';
import { TodoContainer } from '../features';

export default () => (
	<main>
		<h2>Welcome to "typesafe-actions" reference project implementation!</h2>
		<TodoContainer />
	</main>
);
